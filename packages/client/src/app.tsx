import React from 'react';
import ReactDOM from 'react-dom';
import axios from 'axios';

const App = () => {
    return (
        <div>Cocktail App</div>
    );
}

ReactDOM.render(<App />, document.getElementById("root"));